# 更新日志

## v1.0.1 (2026-09-19)

- 修复 CI：`dev/tools/build_bangumi_db.py` 随仓库分发（此前被 dev 忽略规则误排除，
  Actions 首次运行报"文件不存在"）——本版起周更流水线实测跑通，首个自动数据包
  `data-2026-09-15` 已发布；
- 配置页「已加载知识库」改为**打开配置页时实时检测**：显示 bangumi 条数与
  **数据日期**（换过 kb_data 后不再停留旧值）。

## v1.0.0 (2026-09-19)

- 首版：自带离线知识数据、注册四个 Tool 供 LLM 按需查询的知识库插件：
  - `search_knowledge`：萌娘百科多主题（崩坏：星穹铁道 / 绝区零 / 原神 / 鸣潮 /
    异环 / 明日方舟：终末地 + 萌战赛事）jieba + BM25 检索，约 2 ms/查询；
  - `search_bangumi` / `get_bangumi_season` / `browse_bangumi`：bangumi 资料库
    （约 25 万条动画/漫画/游戏/音乐/三次元及角色、人物，SQLite FTS5）——
    条目检索、季度新番列表（按追番人数排序）、按标签/类型/年份/评分筛选；
- 检索特性：正文与小节标题分开打分（标题命中加权）、简称按字符近似兜底
  （"京吹" →《吹响吧！上低音号》）、查询剥离疑问词、命中位置开窗截断；
  角色直查置顶基本信息、"XX 有哪些角色"命中全量名单、萌战问法自动换算年份；
- 索引后台加载（不阻塞宿主启动），加载期间检索短暂等待（上限 15 秒）；
  数据缺失或单个索引文件损坏温和降级为"未收录"提示，不作为错误上报；
- `kb_data/`（约 400 MB）不入库，随插件包分发；`/kb_stats` 可查看
  萌娘百科片段数与 bangumi 条目数、构建时间，以及 **bangumi 数据源日期**
  （构建时写入 meta 表，`--source-date` 可指定；旧库回退为文件日期）；
- 配置页检索参数（默认返回条数 / 片段字符上限）提供中文标签与小字说明；
  配置更新即时生效——索引与配置无关，不再触发重载；
- `sdk.min_version` 抬到 2.8.1（组织基线）。
